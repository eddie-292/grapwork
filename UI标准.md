# UI 设计标准

## 基本要求
- 不要使用渐变色
- 不要使用emoji表情，应使用SVG

## 字体

### 字体栈

```css
font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'SF Pro Text', 'Helvetica Neue', 'Segoe UI', Roboto, sans-serif;
```

### 代码字体

```css
font-family: 'SF Mono', Monaco, 'Andale Mono', monospace;
```

### 字号规范

| 元素 | 字号 | 字重 | 颜色 |
|------|------|------|------|
| 面板标题 | 14px | 600 | `#1d1d1f` |
| 章节标题 | 12px | 600 | `#86868b` |
| 主标题 | 14px | 600 | `#1d1d1f` |
| 正文/气泡 | 14px | 400 | - |
| 按钮文本 | 13px | 500 | - |
| 标签文本 | 13px | 400 | `#86868b` |
| 时间戳 | 11px | 400 | `#a1a1a6` |
| 工具调用 | 12px | - | - |
| 工具图标 | 10px | - | - |
| 代码块 | 13px | - | - |
| 行内代码 | 0.9em | - | - |

### 行高

- 正文：`1.5`
- 标题：`1.3`

---

## 颜色系统

### 主色调

| 用途 | 色值 |
|------|------|
| 主色 | `#007aff` |
| 主色悬停 | `#0056cc` |
| 主色浅色背景 | `rgba(0, 122, 255, 0.1)` |

### 背景色

| 用途 | 色值 |
|------|------|
| 主背景 | `#fbfbfd` |
| 面板背景 | `#ffffff` |
| 次级背景 | `#f2f2f7` |
| 三级背景 | `#f5f5f7` |
| 悬停背景 | `rgba(0, 122, 255, 0.05)` |

### 文本色

| 用途 | 色值 |
|------|------|
| 主文本 | `#1d1d1f` |
| 次要文本 | `#86868b` |
| 弱化文本 | `#a1a1a6` |
| 主色文本 | `#ffffff` |

### 边框色

| 用途 | 色值 |
|------|------|
| 默认边框 | `rgba(60, 60, 67, 0.12)` |
| 悬停边框 | `rgba(60, 60, 67, 0.29)` |

### 状态色

| 状态 | 背景色 | 文本色 |
|------|--------|--------|
| 进行中 | `rgba(0, 122, 255, 0.1)` | `#007aff` |
| 完成 | `rgba(52, 199, 89, 0.15)` | `#34c759` |
| 失败 | `rgba(255, 59, 48, 0.1)` | `#ff3b30` |
| 空闲 | `rgba(142, 142, 147, 0.12)` | `#8e8e93` |

---

## 按钮

### 主按钮 (发送、播放)

| 属性 | 值 |
|------|-----|
| 背景色 | `#007aff` |
| 悬停背景 | `#0056cc` |
| 禁用背景 | `#c7c7cc` |
| 尺寸 | `44px × 44px` |
| 圆角 | `50%` (圆形) |
| 图标尺寸 | `18px × 18px` |
| 过渡 | `250ms cubic-bezier(0.25, 0.1, 0.25, 1)` |
| 阴影 | `0 2px 8px rgba(0, 122, 255, 0.25)` |
| 悬停阴影 | `0 4px 16px rgba(0, 122, 255, 0.35)` |
| 悬停位移 | `translateY(-1px)` |

### 次级按钮

| 属性 | 值 |
|------|-----|
| 背景色 | `#f2f2f7` |
| 悬停背景 | `#e5e5ea` |
| 内边距 | `10px 16px` |
| 圆角 | `10px` |
| 字体 | 13px |
| 边框 | `1px solid rgba(60, 60, 67, 0.12)` |
| 悬停位移 | `translateY(-1px)` |

### 新建聊天按钮

| 属性 | 值 |
|------|-----|
| 背景色 | `#f2f2f7` |
| 边框 | `1px dashed rgba(60, 60, 67, 0.12)` |
| 悬停边框 | `rgba(60, 60, 67, 0.29)` |
| 圆角 | `10px` |
| 宽度 | `calc(100% - 24px)` |
| 字体 | 13px |
| 颜色 | `#86868b` |

### 语音/录音按钮

| 状态 | 背景色 | 边框色 | 文本色 |
|------|--------|--------|--------|
| 默认 | `#ffffff` | `rgba(60, 60, 67, 0.12)` | `#1d1d1f` |
| 悬停 | `rgba(0, 122, 255, 0.05)` | `#007aff` | `#007aff` |
| 录音中 | `#fff5f5` | `#ffcccb` | `#cc0000` |

| 属性 | 值 |
|------|-----|
| 内边距 | `12px` |
| 圆角 | `10px` |
| 字体 | 13px |
| 图标尺寸 | `16px × 16px` |

---

## 阴影系统

| 名称 | 值 |
|------|-----|
| sm | `0 1px 2px rgba(0, 0, 0, 0.04)` |
| md | `0 4px 12px rgba(0, 0, 0, 0.08)` |
| lg | `0 12px 40px rgba(0, 0, 0, 0.12)` |
| xl | `0 24px 60px rgba(0, 0, 0, 0.16)` |

---

## 过渡动画

| 名称 | 时长 | 缓动函数 |
|------|------|----------|
| fast | 150ms | `cubic-bezier(0.25, 0.1, 0.25, 1)` |
| normal | 250ms | `cubic-bezier(0.25, 0.1, 0.25, 1)` |
| smooth | 350ms | `cubic-bezier(0.25, 0.1, 0.25, 1)` |
| spring | 400ms | `cubic-bezier(0.34, 1.56, 0.64, 1)` |

---

## 线条 (边框与分隔)

### 面板分隔

```css
border: 1px solid rgba(60, 60, 67, 0.12);
```

### 输入框边框

| 状态 | 颜色 |
|------|------|
| 默认 | `rgba(60, 60, 67, 0.12)` |
| 焦点 | `#007aff` |
| 焦点光晕 | `0 0 0 3px rgba(0, 122, 255, 0.1)` |

### 按钮边框

| 类型 | 边框 |
|------|------|
| 次级按钮 | `1px solid rgba(60, 60, 67, 0.12)` |
| 新建聊天 | `1px dashed rgba(60, 60, 67, 0.12)` |
| 语音按钮 | `1px solid rgba(60, 60, 67, 0.12)` |

### 聊天气泡边框

| 类型 | 边框 |
|------|------|
| 工具调用 | `1px solid rgba(60, 60, 67, 0.12)` |
| 工具结果内容 | `1px solid rgba(60, 60, 67, 0.12)` |
| 引用 | `3px solid rgba(60, 60, 67, 0.29)` (左侧) |

### 表格边框

```css
border: 1px solid rgba(60, 60, 67, 0.12);
```

---

## 输入框

### 聊天输入框

| 属性 | 值 |
|------|-----|
| 内边距 | `12px 16px` |
| 边框 | `1px solid rgba(60, 60, 67, 0.12)` |
| 焦点边框 | `#007aff` |
| 焦点光晕 | `0 0 0 3px rgba(0, 122, 255, 0.1)` |
| 圆角 | `24px` |
| 字体 | 14px |
| 最大高度 | `120px` |
| 行高 | `1.5` |
| 宽度 | `flex: 1` |

### 设置输入框

| 属性 | 值 |
|------|-----|
| 宽度 | `100%` |
| 内边距 | `10px 12px` |
| 边框 | `1px solid rgba(60, 60, 67, 0.12)` |
| 焦点边框 | `#007aff` |
| 圆角 | `10px` |
| 字体 | 13px |

### 文本域 (TTS 输入)

| 属性 | 值 |
|------|-----|
| 宽度 | `100%` |
| 内边距 | `12px` |
| 边框 | `1px solid rgba(60, 60, 67, 0.12)` |
| 焦点边框 | `#007aff` |
| 圆角 | `10px` |
| 字体 | 13px |
| 最小高度 | `100px` |
| 调整 | `vertical` |

---

## 颜色速查

| 用途 | 色值 |
|------|------|
| 主背景 | `#fbfbfd` |
| 面板背景 | `#ffffff` |
| 主文本 | `#1d1d1f` |
| 次要文本 | `#86868b` |
| 弱化文本 | `#a1a1a6` |
| 占位符 | `#a1a1a6` |
| 边框 | `rgba(60, 60, 67, 0.12)` |
| 主按钮 | `#007aff` |
| 次级按钮 | `#f2f2f7` |
| 激活状态 | `rgba(0, 122, 255, 0.1)` |
| 录音状态 | `#fff5f5` / `#cc0000` |
| 链接 | `#007aff` |

---

## 圆角规范

| 元素 | 圆角值 |
|------|--------|
| 主按钮 | `50%` (圆形) |
| 次级按钮 | `10px` |
| 聊天输入框 | `24px` |
| 设置输入框 | `10px` |
| 聊天气泡 | `16px` |
| 历史项 | `10px` |
| 工具调用图标 | `6px` |
| 代码块 | `10px` |
| 行内代码 | `4px` |

---

## 交互效果

### 按钮悬停效果
- 向上位移 `translateY(-1px)`
- 阴影增强
- 边框颜色变化

### 按钮点击效果
- 缩小 `scale(0.98)`
- 阴影减弱

### 卡片悬停效果
- 向上位移 `translateY(-2px)`
- 阴影增强

### 页面切换
- 淡入淡出 + 位移
- 过渡时长 300ms

---

## 滚动条样式

```css
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: rgba(60, 60, 67, 0.29);
  border-radius: 9999px;
}

::-webkit-scrollbar-thumb:hover {
  background: #a1a1a6;
}
```

---

## 玻璃拟态 (可选)

```css
.glass {
  background: rgba(255, 255, 255, 0.72);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.24);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
}
```
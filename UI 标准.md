# UI 设计标准

## 基本要求
- 不要使用渐变色
- 不要使用emoji表情，应使用SVG

## 字体

### 字体栈

```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
```

### 代码字体

```css
font-family: 'SF Mono', Monaco, 'Andale Mono', monospace;
```

### 字号规范

| 元素 | 字号 | 字重 | 颜色 |
|------|------|------|------|
| 面板标题 | 14px | 600 | `#333` |
| 章节标题 | 12px | 600 | `#999` |
| 主标题 | 14px | 600 | `#333` |
| 正文/气泡 | 14px | 400 | - |
| 按钮文本 | 13px | 400 | - |
| 标签文本 | 13px | 400 | `#666` |
| 时间戳 | 11px | 400 | `#999` |
| 工具调用 | 12px | - | - |
| 工具图标 | 10px | - | - |
| 代码块 | 13px | - | - |
| 行内代码 | 0.9em | - | - |

### 行高

- 正文：`1.5`
- 标题：`1.3`

---

## 按钮

### 主按钮 (发送、播放)

| 属性 | 值 |
|------|-----|
| 背景色 | `#333` |
| 悬停背景 | `#555` |
| 禁用背景 | `#ccc` |
| 尺寸 | `44px × 44px` |
| 圆角 | `50%` (圆形) |
| 图标尺寸 | `18px × 18px` |
| 过渡 | `0.2s` |

### 次级按钮

| 属性 | 值 |
|------|-----|
| 背景色 | `#f5f5f5` |
| 悬停背景 | `#eee` |
| 内边距 | `10px` / `12px` |
| 圆角 | `8px` |
| 字体 | 13px |

### 新建聊天按钮

| 属性 | 值 |
|------|-----|
| 背景色 | `#f5f5f5` |
| 边框 | `1px dashed #ddd` |
| 悬停边框 | `#ccc` |
| 圆角 | `8px` |
| 宽度 | `calc(100% - 24px)` |
| 字体 | 13px |
| 颜色 | `#666` |

### 语音/录音按钮

| 状态 | 背景色 | 边框色 | 文本色 |
|------|--------|--------|--------|
| 默认 | `#fff` | `#eee` | `#333` |
| 悬停 | `#eee` | `#eee` | `#333` |
| 录音中 | `#fee` | `#fcc` | `#c00` |

| 属性 | 值 |
|------|-----|
| 内边距 | `12px` |
| 圆角 | `8px` |
| 字体 | 13px |
| 图标尺寸 | `16px × 16px` |

---

## 线条 (边框与分隔)

### 面板分隔

```css
border: 1px solid #eee;
```

### 输入框边框

| 状态 | 颜色 |
|------|------|
| 默认 | `#eee` |
| 焦点 | `#ccc` |

### 按钮边框

| 类型 | 边框 |
|------|------|
| 次级按钮 | `1px solid #eee` |
| 新建聊天 | `1px dashed #ddd` |
| 语音按钮 | `1px solid #eee` |

### 聊天气泡边框

| 类型 | 边框 |
|------|------|
| 工具调用 | `1px solid #e9ecef` |
| 工具结果内容 | `1px solid #eee` |
| 引用 | `3px solid #ccc` (左侧) |

### 表格边框

```css
border: 1px solid #ddd;
```

---

## 输入框

### 聊天输入框

| 属性 | 值 |
|------|-----|
| 内边距 | `12px 16px` |
| 边框 | `1px solid #eee` |
| 焦点边框 | `#ccc` |
| 圆角 | `24px` |
| 字体 | 14px |
| 最大高度 | `120px` |
| 行高 | `1.5` |
| 宽度 | `flex: 1` |

### 设置输入框

| 属性 | 值 |
|------|-----|
| 宽度 | `100%` |
| 内边距 | `10px 12px` |
| 边框 | `1px solid #eee` |
| 焦点边框 | `#ccc` |
| 圆角 | `8px` |
| 字体 | 13px |

### 文本域 (TTS 输入)

| 属性 | 值 |
|------|-----|
| 宽度 | `100%` |
| 内边距 | `12px` |
| 边框 | `1px solid #eee` |
| 焦点边框 | `#ccc` |
| 圆角 | `8px` |
| 字体 | 13px |
| 最小高度 | `100px` |
| 调整 | `vertical` |

---

## 颜色速查

| 用途 | 色值 |
|------|------|
| 主背景 | `#fafafa` |
| 面板背景 | `#fff` |
| 主文本 | `#333` |
| 次要文本 | `#666` |
| 弱化文本 | `#888` |
| 占位符 | `#999` |
| 边框 | `#eee` |
| 主按钮 | `#333` |
| 次级按钮 | `#f5f5f5` |
| 激活状态 | `#e8f4fd` |
| 录音状态 | `#fee` / `#c00` |
| 链接 | `#4a90d9` |

---

## 圆角规范

| 元素 | 圆角值 |
|------|--------|
| 主按钮 | `50%` (圆形) |
| 次级按钮 | `8px` |
| 聊天输入框 | `24px` |
| 设置输入框 | `8px` |
| 聊天气泡 | `16px` |
| 历史项 | `8px` |
| 工具调用图标 | `4px` |
| 代码块 | `8px` |
| 行内代码 | `3px` |
